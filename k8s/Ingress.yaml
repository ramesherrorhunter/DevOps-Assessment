apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nodejs-ingress
  namespace: production
spec:
  rules:
    - host: devops.local
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: nodejs-service
                port:
                  number: 80
 

